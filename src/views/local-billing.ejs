<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Billing - Local</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            line-height: 1.6;
        }

        .heading {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .container {
            display: grid;
            grid-template-columns: auto auto auto;
            gap: 10px 20px;
        }

        .field-name {
            font-weight: bold;
            text-align: right;
        }

        .colon {
            text-align: center;
        }

        .field-value {
            text-align: left;
        }
    </style>
</head>

<body>
    <div class="heading">Billing - Local</div>
    <div class="container">
        <% for (const [key, value] of Object.entries(data)) { %>
            <div class="field-name">
                <%= key %>
            </div>
            <div class="colon">:</div>
            <div class="field-value">
                <%= value %>
            </div>
            <% } %>
    </div>
</body>

</html>